# ============================================================
# PaperAI Backend Dependencies
# 智能论文精读 AI Agent 系统
# Updated: 2026-08 — verified against latest PyPI releases
# ============================================================

# --- Web Framework ---
fastapi>=0.141.0,<1.0.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.18

# --- Configuration & Validation ---
pydantic>=2.13.0,<3.0.0
pydantic-settings>=2.7.0,<3.0.0

# --- Database (Async SQLAlchemy 2.0+) ---
sqlalchemy[asyncio]>=2.0.51,<2.1.0
aiosqlite>=0.21.0
asyncpg>=0.30.0

# --- Redis ---
redis>=5.2.0,<6.0.0

# --- LangChain & LangGraph (Agent 编排) ---
langchain>=1.3.0,<2.0.0
langchain-core>=1.4.0,<2.0.0
langchain-community>=0.3.0,<1.0.0
langgraph>=1.2.0,<2.0.0
langchain-openai>=1.4.0,<2.0.0

# --- Vector Database (RAG) ---
# ⚠️ ChromaDB 1.x 是 Rust 重写版, API 有破坏性变更:
#    旧: chromadb.Settings(persist_directory=...)  →  新: chromadb.PersistentClient(path=...)
chromadb>=1.5.0,<2.0.0

# --- LLM Provider SDKs ---
openai>=2.50.0

# --- PDF Processing ---
pypdf>=5.0.0
pdfplumber>=0.11.0

# --- JWT Authentication ---
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# --- Utilities ---
python-dotenv>=1.0.1
httpx>=0.28.0

# --- Logging ---
loguru>=0.7.2

# --- Testing ---
pytest>=8.3.0
pytest-asyncio>=0.24.0
